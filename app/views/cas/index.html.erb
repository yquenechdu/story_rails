<h1>Listing cas</h1>

<table>
  <tr>
    <th>Criteria</th>
    <th>Story</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cas.each do |ca| %>
  <tr>
    <td><%= ca.criteria %></td>
    <td><%= ca.story_id %></td>
    <td><%= link_to 'Show', ca %></td>
    <td><%= link_to 'Edit', edit_ca_path(ca) %></td>
    <td><%= link_to 'Destroy', ca, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ca', new_ca_path %>
